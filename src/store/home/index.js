// 仓库存数据的地方
const state = {};
// 修改state的唯一地方
const mutations = {};
// 书写业务逻辑，处理异步云
const actions = {};
// 功能类似计算属性，用于少些写东西
const getters = {};

// 对外暴露Store类的一个实例对象
export default {
    state,
    mutations,
    actions,
    getters,
};
